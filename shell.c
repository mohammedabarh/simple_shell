#include "shell.h"

/**
 * main - Serves as the entry point for the application
 *
 * Returns: 0 if the program completes successfully
 *
 */

int main(void)
{
	while(1)
	{
		readcmd();
	}

	return (0);
}

